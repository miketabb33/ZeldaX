function priestStatBoost(player){
  player.baseIntellect += 2;
  player.baseSpirit += 3;
}

function rogueStatBoost(player){
  player.baseStrength += 1;
  player.baseAgility += 3;
  player.baseStamina += 1;
}

function warriorStatBoost(player){
  player.baseStrength += 3;
  player.baseStamina += 2;
}

function mageStatBoost(player){
  player.baseIntellect += 3;
  player.baseSpirit += 2;
}

function druidStatBoost(player){
  player.baseStrength += 1
  player.baseIntellect += 2;
  player.baseSpirit += 2;
}

function druidStatBoost(player){
  player.baseStrength += 1
  player.baseIntellect += 2;
  player.baseSpirit += 2;
}

function hunterStatBoost(player){
  player.baseAgility += 3;
  player.baseStamina += 1;
  player.baseSpirit += 1;
}

function warlockStatBoost(player){
  player.baseStamina += 1;
  player.baseIntellect += 2;
  player.baseSpirit += 2;
}

function shamanStatBoost(player){
  player.baseStrength += 1;
  player.baseStamina += 1;
  player.baseIntellect += 1;
  player.baseSpirit += 1;
}

function paladinStatBoost(player){
  player.baseStrength +=2
  player.baseStamina += 2;
  player.baseSpirit += 1;
}
